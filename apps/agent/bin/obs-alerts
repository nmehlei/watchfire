#!/usr/bin/env bash
set -euo pipefail
here="$(cd "$(dirname "$0")" && pwd)"
repo="$(dirname "$here")"
if [[ -f "$repo/dist/adapters/observability/cli-alerts.js" ]]; then
  exec node "$repo/dist/adapters/observability/cli-alerts.js" "$@"
elif [[ -f "$repo/src/adapters/observability/cli-alerts.ts" ]]; then
  exec npx --no-install tsx "$repo/src/adapters/observability/cli-alerts.ts" "$@"
else
  echo "obs-alerts: neither dist/ nor src/ cli found" >&2
  exit 2
fi
